# Лабораторная работа №1
## Вариант: М2

## Теоретическая часть

### Обратная польская нотация (ОПН)
Обратная польская нотация (Reverse Polish Notation) - это математическая нотация, в которой операторы следуют за своими операндами. Например:
- Инфиксная запись: `2 + 3`
- ОПН: `2 3 +`

### Алгоритм сортировочной станции (Shunting Yard)
Алгоритм, разработанный Эдсгером Дейкстрой, для преобразования инфиксной записи в ОПН. Основные правила:
1. Операнды сразу добавляются в выходную строку
2. Операторы помещаются в стек с учетом приоритета
3. Открывающие скобки помещаются в стек
4. При встрече закрывающей скобки выталкиваются операторы до открывающей скобки

* **Цель:** Написать калькулятор на Python, прозводящи вычисления с помощью перевода входного выражения в RPN(обратную(постфиксную) польскую нотацию), используя для этого алгоритм шунтирующего двора(Shunting Yard)
* **Библиотеки:** *os*, *re*, *sys*, *pytest*
* **Допущения:**
  - Выражение может содержать любое количество пробелов между числами и операторами, поскольку они игнорируются.
  - Любые символы кроме "0123456789-+*/()%. " в выражении запрещены
  - Унарный '-' заменяется на '~'
  - Унарный '+' игнорируется, из исходного выражения убирается
  - Если перед унарной операцией следовала другая операция или унарный оператор заключен в скобки с операндом, к которому он применен, разницы не будет, но лучше записывать в скобках для читаемости выражения
  - Число в программе всегда представляется в виде числа типа float, округляется только результат
  - Операции '//' и  '%' запрещены для вещественных чисел
  - 0**0 не определен

* **Хронология взаимодействия пользователя и программы**
    - Пользователь вводит выражение (Правила ввода: 1. операторы можно разделять пробелами при желании; 2. Унарный минус желательно записывать в скобках для однозначного определения выражения)
    - Программа выполняет валидацию входного выражения путем проверки на наличие символов, не имеющих значение
    - Программа токенизирует выражения, разбивая его на операторы и операнды
    - Программа переводит набор токенов в RPN, возвращая строку RPN
    - Программа выполняет вычисление стеком
    - При ошибке программа перехватывает соответствующее исключение и выводит 'Ошибка' в терминал
    - При успешном вычислении пользователь получает вывод в формате: "{Входное выражение}={Результат}"


* **Чему я научился:**
    - Написание простых автотестов с помощью pytest
    - Разделение логики по функциям и модулям
    - работа с виртуальным окружением venv
    - Понимание принципа работы такой структуры данных, как стек
    - Работа со стеком
    - Отладка проекта для выявления неисправностей
    - Обработка исключение и самостоятельное управление ими

* **Этапы выполнения работы (абстрактный список этапов выполнения)** 
    - Создание файловой системы
    - подключение виртуального окружения к проекту
    - установка пакетов
    - написание основной логики, разбиение по модулям
    - ручное тестирование, импровизация тест-кейсов
    - организация авто-тестов с помощью библиотеки pytest
    - тестирование, анализ результатов, отладка
    - можно пользоваться

## Инструкция к исользованию
### клонировать репозиторий и установить библиотеки
- Клонировать
```git clone https://github.com/Dinar2806/lab1-M2_level.git```
- Перейит в корень проекта
```cd lab1-M2_level```
- установить пакеты и библиотеки
```pip install -r requirements.txt```


### Запуск программы
```python -m src.main```


